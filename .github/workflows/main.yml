name: "Ultimate APK Builder (All-In-One)"

on:
  repository_dispatch:
    types: [build-app]
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: "Checkout Code"
        uses: actions/checkout@v4

      # 1. پراجیکٹ کا بنیادی ڈھانچہ (Project Structure)
      - name: "Initialize Project Folders"
        run: |
          mkdir -p app/src/main/java
          mkdir -p app/src/main/res/layout
          mkdir -p app/src/main/res/values
          mkdir -p app/src/main/res/drawable
          mkdir -p app/src/main/assets

      # 2. کوڈ اور ریسورسز کو محفوظ کرنا (Safe injection using EOF)
      - name: "Inject Payload Data"
        id: prep
        run: |
          # پیکیج اور ایپ کا نام سیٹ کریں
          PKG="${{ github.event.client_payload.package_name || 'com.systempro.app' }}"
          APP_NAME="${{ github.event.client_payload.app_name || 'SystemPro' }}"
          PKG_PATH=$(echo "$PKG" | tr '.' '/')
          mkdir -p "app/src/main/java/$PKG_PATH"

          # محفوظ طریقے سے فائلز لکھنا (تاکہ XML ٹیگز ایرر نہ دیں)
          cat <<'EOF' > "app/src/main/java/$PKG_PATH/MainActivity.java"
          ${{ github.event.client_payload.java }}
          EOF

          cat <<'EOF' > app/src/main/res/layout/activity_main.xml
          ${{ github.event.client_payload.xml }}
          EOF

          cat <<'EOF' > app/src/main/AndroidManifest.xml
          ${{ github.event.client_payload.manifest }}
          EOF

          # UI تھیم اور کلرز (اگر پے لوڈ میں موجود ہوں)
          if [ -n "${{ github.event.client_payload.colors }}" ]; then
            cat <<'EOF' > app/src/main/res/values/colors.xml
            ${{ github.event.client_payload.colors }}
          EOF
          fi

          if [ -n "${{ github.event.client_payload.styles }}" ]; then
            cat <<'EOF' > app/src/main/res/values/styles.xml
            ${{ github.event.client_payload.styles }}
          EOF
          fi

          # پیکیج کا نام تمام جاوا فائلز میں خودکار ٹھیک کریں
          find app/src/main/java -name "*.java" -exec sed -i "s/package .*;/package $PKG;/" {} \;
          
          echo "PKG=$PKG" >> $GITHUB_OUTPUT
          echo "APP_NAME=$APP_NAME" >> $GITHUB_OUTPUT

      # 3. گریڈل اور ڈیپنڈینسیز کی ترتیب (Build Configuration)
      - name: "Generate Build Files"
        run: |
          DEPS="${{ github.event.client_payload.dependencies }}"
          if [ -z "$DEPS" ]; then
            DEPS="implementation 'androidx.appcompat:appcompat:1.6.1'\n    implementation 'com.google.android.material:material:1.11.0'"
          fi

          cat > build.gradle <<EOF
          plugins { id 'com.android.application' version '8.2.0' apply false }
          EOF

          cat > settings.gradle <<EOF
          pluginManagement { repositories { google(); mavenCentral(); gradlePluginPortal() } }
          dependencyResolutionManagement { 
              repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
              repositories { google(); mavenCentral() } 
          }
          include ':app'
          EOF

          cat > app/build.gradle <<EOF
          plugins { id 'com.android.application' }
          android {
              namespace '${{ steps.prep.outputs.PKG }}'
              compileSdk 34
              defaultConfig {
                  applicationId '${{ steps.prep.outputs.PKG }}'
                  minSdk 24
                  targetSdk 34
                  versionCode 1
                  versionName "1.0"
              }
              buildFeatures { viewBinding true }
          }
          dependencies {
              $DEPS
          }
          EOF

      # 4. اینڈرائیڈ ایس ڈی کے اور جاوا سیٹ اپ
      - name: "Setup Java"
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # 5. بلڈ پروسیس
      - name: "Build APK"
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug --stacktrace

      # 6. آرٹفیکٹ اپ لوڈ (Download Link)
      - name: "Upload APK"
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.prep.outputs.APP_NAME }}
          path: app/build/outputs/apk/debug/*.apk

      # 7. صفائی کا عمل (Cleaning Process)
      - name: "Post-Build Cleanup"
        if: always()
        run: |
          echo "Cleaning up all build files and sensitive code..."
          rm -rf app/src/main/java/*
          rm -rf app/src/main/res/layout/*
          rm -rf app/src/main/res/values/*
          rm -rf app/build/
          rm -rf .gradle/
          echo "Cleanup finished."
