name: System-Pro-App-Build

on:
  repository_dispatch:
    types: [build-apk] # یہ وہی ایونٹ ہے جو آپ کی ایپ (JS) بھیج رہی ہے

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Auto-Process Input Data
        run: |
          # 1. فولڈرز کی تیاری
          mkdir -p app/src/main/assets
          mkdir -p app/src/main/res/drawable
          mkdir -p app/src/main/java/com/systempro/app

          # 2. ایپ کا نام اور ایچ ٹی ایم ایل سیٹ کرنا
          APP_NAME="${{ github.event.client_payload.app_name }}"
          echo "${{ github.event.client_payload.app_html }}" > app/src/main/assets/index.html

          # 3. آئیکون ہینڈلنگ (اگر ایپ سے آ رہا ہے)
          if [ ! -z "${{ github.event.client_payload.app_icon }}" ]; then
            echo "${{ github.event.client_payload.app_icon }}" | base64 --decode > app/src/main/res/drawable/app_icon.png
          fi

          # 4. آٹو پیکج نیم (ہر بار نیا)
          BUILD_ID=$(date +%s)
          NEW_PACKAGE="com.systempro.v${BUILD_ID}"
          sed -i "s/applicationId \".*\"/applicationId \"$NEW_PACKAGE\"/" app/build.gradle

          # 5. مینی فیسٹ میں پرمیشنز اور نام ایڈ کرنا
          # یہاں ہم آپ کی ایپ سے آنے والی تمام پرمیشنز کو لوپ میں شامل کریں گے
          PERMS_XML=""
          for perm in ${{ join(github.event.client_payload.permissions, ' ') }}; do
            PERMS_XML="${PERMS_XML}<uses-permission android:name=\"$perm\" />\n"
          done

          cat <<EOF > app/src/main/AndroidManifest.xml
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              ${PERMS_XML}
              <uses-permission android:name="android.permission.INTERNET" />
              <application 
                  android:label="${APP_NAME}" 
                  android:icon="@drawable/app_icon"
                  android:theme="@style/Theme.AppCompat.Light.NoActionBar">
                  <activity android:name="com.systempro.app.MainActivity" android:exported="true">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
              </application>
          </manifest>
          EOF

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Build APK with Gradle
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ github.event.client_payload.app_name }}-Build
          path: app/build/outputs/apk/debug/*.apk
