<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.system.pro">

    <!-- ==================== PERMISSIONS ==================== -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Optional: Flash feature is not required but better if present -->
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />

    <!-- ==================== APPLICATION ==================== -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="System Pro Studio"
        /* 
            THEME SELECTION:
            - Use LightTheme for bright UI
            - Use DarkTheme for dark UI  
            - Use BlueTheme, GreenTheme, DarkBlueTheme for alternate color schemes
            - For Android 12+ with dynamic color support, use Theme.Material3.DynamicColors.DayNight
            (You must also add the Material3 dependency and call MaterialColors.applyDynamicColor())
        */
        android:theme="@style/LightTheme"
        android:usesCleartextTraffic="true">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            /* You can override the theme for this specific activity: */
            <!-- android:theme="@style/DarkTheme" -->  
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 
            ==================== ADDITIONAL ACTIVITIES ====================
            You can set different themes per activity like this:
            
        <activity
            android:name=".SettingsActivity"
            android:theme="@style/DarkTheme" />
            
        <activity
            android:name=".InfoActivity"
            android:theme="@style/BlueTheme" />
        -->

    </application>

</manifest>
